# CRAN submission comments — lucidr 0.1.0

## Resubmission

This is a resubmission following archival. The following invalid URLs flagged by
CRAN have been fixed:

- `http://127.0.0.1:8080` (README.md): replaced with inline code (backticks) so
  it is not treated as a hyperlink.
- `https://hyperverse-r.github.io/lucidr/` (README.md): the pkgdown site is now
  deployed and returns 200.

## R CMD check results

0 errors | 0 warnings | 1 note

Tested on: macOS aarch64 (Sequoia 15.6.1), R 4.4.2

## Notes

### `checking for future file timestamps`

Unable to verify current time.
